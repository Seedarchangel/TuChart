from distutils.core import setup
setup(
  name = 'tuchart',
  packages = ['tuchart'], # this must be the same as the name above
  version = '0.1',
  description = 'Visualization for Tushare using Echarts',
  author = 'Rong Zhou Li',
  author_email = 'rzli2@illinois.com',
  url = 'hhttps://github.com/Seedarchangel/TuChart', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['echarts', 'tushare', 'pyqt'], # arbitrary keywords
  classifiers = [],
)